---
preset:
  name: kubernetes
  description: "Kubernetes Development - Tools for Kubernetes development and deployment"

apt_packages:
  - curl
  - gnupg2
  - software-properties-common

# Note: kubectl and helm should be installed via their official installation methods
# snap_packages not supported in current schema

pip_packages:
  - kubernetes
  - pykube-ng

npm_packages:
  - kubernetes-yaml-completion

aliases:
  k: "kubectl"
  kgp: "kubectl get pods"
  kgs: "kubectl get services"
  kgd: "kubectl get deployments"
  kdesc: "kubectl describe"
  klog: "kubectl logs"
  kexec: "kubectl exec -it"

ports:
  dashboard: 8080  # Kubernetes dashboard
  api-server: 6443  # Kubernetes API server
  kubelet: 10250 # Kubelet API
  prometheus: 9090  # Prometheus (common in k8s)
  grafana: 3000  # Grafana (common in k8s)

services:
  postgresql:
    enabled: false  # Kubernetes manages its own workloads
  redis:
    enabled: false  # Kubernetes manages its own workloads

environment:
  KUBECONFIG: "~/.kube/config"
  KUBE_EDITOR: "nano"