[package]
name = "vm-provider"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
duct = "0.13"
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.9"
thiserror = "1.0"

# Add vm-config as a local dependency
[dependencies.vm-config]
path = "../vm-config"
