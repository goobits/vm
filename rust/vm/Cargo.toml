[package]
name = "vm"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
clap = { version = "4.0", features = ["derive"] }
serde_yaml = "0.9"
vm-config = { path = "../vm-config" }
vm-provider = { path = "../vm-provider" }
