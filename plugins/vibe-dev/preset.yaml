# Vibe Development Preset
# Uses a pre-built @vibe-box snapshot; enabled AI CLIs are installed at runtime.

# Reference pre-built vibe-box snapshot
vm_box: '@vibe-box'

# Host synchronization configuration (runtime behavior)
host_sync:
  git_config: true
  ai_tools: true  # Enable AI tool sync (Claude, Antigravity, Codex)
