# Vibe Development Preset
# Uses a pre-built @vibe-box snapshot with Antigravity, Claude, and Codex.

# Reference pre-built vibe-box snapshot
vm_box: '@vibe-box'

tools:
  updates: prompt
  agent-skills: {}

# Host synchronization configuration (runtime behavior)
host_sync:
  git_config: true
  ai_tools: true  # Sync AI state; the Vibe base owns the CLI executables.
